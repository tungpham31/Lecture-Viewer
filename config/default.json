{
  "Media" : {
    /*
     * root - where we can find the media (video content). This can
     * be overridden in the specific ./{host}.json file for the
     * specific host if necessary.
     */
    "root" : "/media/uploaded"
  },

  "User" : {
    /*
     * root - where we can find the user information. This can be
     * overridded in the specific ./{host}.json file for the
     * specific host if necessary.
     */
    "root" : "./users"
  },

  "SSL" : {
    "root" : "./ssl"
  }
}
